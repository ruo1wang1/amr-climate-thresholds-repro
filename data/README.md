# Metadata

- `data_dictionary.csv` provides variable definitions, units/sources, and aggregation notes for the derived panel datasets in `data/derived/`.
- Variables are harmonized at the country/region–year level for reproducibility.

